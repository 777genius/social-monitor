# Ingestion Context

Bounded context placeholder for scan jobs, source provider adapters, cursors and normalized source items.

Layout is fixed as:

- `domain`
- `features`
- `ports`
- `adapters`
- `interfaces`
