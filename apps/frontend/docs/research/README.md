# Frontend Research

Research documents for Flutter, responsive/adaptive UI, design-system choices, frontend package selection and implementation tradeoffs.

Current implementation note:

- `headless_adaptive` is no longer a local frontend workspace package. Its source of truth is `777genius/flutter_headless`, package path `packages/headless_adaptive`, consumed by `social_monitor_design_system` from pinned commit `eda0637`.

Current research:

- `flutter-adaptive-tooling-research-2026.md` - adaptive layout tooling, package choices and responsive frontend architecture.
- `headless-adaptive-ecosystem-research-2026.md` - package strategy, competitor analysis and clean architecture for `headless_adaptive`.
