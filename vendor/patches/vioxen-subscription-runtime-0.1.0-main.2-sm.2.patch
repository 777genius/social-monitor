diff --git a/dist/agent-task/codec.js b/dist/agent-task/codec.js
--- a/dist/agent-task/codec.js
+++ b/dist/agent-task/codec.js
@@ -381,8 +381,8 @@ function parseUsage(value, path) {
     const input = objectAt(value, path);
     return {
-        ...optionalPositiveIntegerField(input, "inputTokens", `${path}.inputTokens`),
-        ...optionalPositiveIntegerField(input, "outputTokens", `${path}.outputTokens`),
-        ...optionalPositiveIntegerField(input, "totalTokens", `${path}.totalTokens`),
+        ...optionalNonNegativeSafeIntegerField(input, "inputTokens", `${path}.inputTokens`),
+        ...optionalNonNegativeSafeIntegerField(input, "outputTokens", `${path}.outputTokens`),
+        ...optionalNonNegativeSafeIntegerField(input, "totalTokens", `${path}.totalTokens`),
     };
 }
 function parseCost(value, path) {
@@ -513,6 +513,11 @@ function optionalNonNegativeNumberField(input, key, path) {
         return {};
     return { [key]: nonNegativeNumberAt(input[key], path) };
 }
+function optionalNonNegativeSafeIntegerField(input, key, path) {
+    if (input[key] === undefined)
+        return {};
+    return { [key]: nonNegativeSafeIntegerAt(input[key], path) };
+}
 function assertProtocolVersion(value, path) {
     if (value !== agentTaskProtocolVersion) {
         throw protocolError("agent_task_protocol_version_invalid", `${path} must be ${agentTaskProtocolVersion}`);
@@ -565,6 +570,12 @@ function nonNegativeNumberAt(value, path) {
     }
     return value;
 }
+function nonNegativeSafeIntegerAt(value, path) {
+    if (!Number.isSafeInteger(value) || value < 0) {
+        throw protocolError("agent_task_result_invalid", `${path} must be a non-negative safe integer`);
+    }
+    return value;
+}
 function protocolError(code, message) {
     return new AgentTaskProtocolError(code, message);
 }
diff --git a/dist/provider-codex/codex-app-server-execution-engine.js b/dist/provider-codex/codex-app-server-execution-engine.js
--- a/dist/provider-codex/codex-app-server-execution-engine.js
+++ b/dist/provider-codex/codex-app-server-execution-engine.js
@@ -147,6 +147,7 @@ export class CodexAppServerExecutionEngine {
         return {
             outputText,
             warnings: result.warnings,
+            ...(result.usage ? { usage: result.usage } : {}),
         };
     }
     async ensureSlot(input) {
@@ -281,6 +282,7 @@ class CodexAppServerClient {
         return {
             outputText: turn.outputText,
             warnings,
+            ...(turn.usage ? { usage: turn.usage } : {}),
         };
     }
     async runGoal(input) {
@@ -609,7 +611,13 @@ class CodexAppServerClient {
                 reject(new Error(`codex_app_server_turn_aborted:${turnId}`));
             };
             input.abortSignal.addEventListener("abort", abort, { once: true });
-            const turn = existing ?? createTurnState();
+            const turn = existing?.threadId === null ||
+                existing?.threadId === input.threadId
+                ? existing
+                : createTurnState(turnId, input.threadId);
+            if (turn.threadId === null) {
+                turn.threadId = input.threadId;
+            }
             turn.waiters.push((state) => {
                 clearTimeout(timer);
                 input.abortSignal.removeEventListener("abort", abort);
@@ -666,6 +671,17 @@ class CodexAppServerClient {
             turn.outputText += stringField(params, "delta") ?? "";
             return;
         }
+        if (record.method === "thread/tokenUsage/updated") {
+            const threadId = stringField(params, "threadId");
+            const turnId = stringField(params, "turnId");
+            if (!threadId || !turnId)
+                return;
+            const state = this.ensureTurn(turnId, threadId);
+            if (state.threadId !== threadId)
+                return;
+            state.usage = readExactTurnUsage(params?.tokenUsage);
+            return;
+        }
         if (record.method === "turn/started") {
             const threadId = stringField(params, "threadId");
             const turn = readRecord(params?.turn);
@@ -746,15 +762,18 @@ class CodexAppServerClient {
             },
         })}\n`);
     }
-    ensureTurn(turnId) {
+    ensureTurn(turnId, threadId = null) {
         if (!turnId)
             return createTurnState();
         const canonicalTurnId = this.turnIdAliases.get(turnId) ?? turnId;
         let turn = this.turns.get(canonicalTurnId);
         if (!turn) {
-            turn = createTurnState();
+            turn = createTurnState(canonicalTurnId, threadId);
             this.turns.set(canonicalTurnId, turn);
         }
+        else if (turn.threadId === null && threadId !== null) {
+            turn.threadId = threadId;
+        }
         return turn;
     }
     resolveTurn(turn) {
@@ -772,14 +791,36 @@ function spawnCodexAppServerProcess(input) {
     });
     return child;
 }
-function createTurnState() {
+function createTurnState(turnId = null, threadId = null) {
     return {
+        turnId,
+        threadId,
         outputText: "",
         completed: false,
         error: null,
+        usage: undefined,
         waiters: [],
     };
 }
+function readExactTurnUsage(value) {
+    const tokenUsage = readRecord(value);
+    const last = readRecord(tokenUsage?.last);
+    if (!last)
+        return undefined;
+    const inputTokens = last.inputTokens;
+    const outputTokens = last.outputTokens;
+    const totalTokens = last.totalTokens;
+    if (!nonNegativeSafeInteger(inputTokens) ||
+        !nonNegativeSafeInteger(outputTokens) ||
+        !nonNegativeSafeInteger(totalTokens) ||
+        totalTokens !== inputTokens + outputTokens) {
+        return undefined;
+    }
+    return { inputTokens, outputTokens, totalTokens };
+}
+function nonNegativeSafeInteger(value) {
+    return Number.isSafeInteger(value) && value >= 0;
+}
 function appServerFallbackWarning(error) {
     return {
         code: "codex_app_server_fallback",
diff --git a/dist/provider-codex/codex-json-agent-driver.js b/dist/provider-codex/codex-json-agent-driver.js
--- a/dist/provider-codex/codex-json-agent-driver.js
+++ b/dist/provider-codex/codex-json-agent-driver.js
@@ -89,6 +89,7 @@ export class CodexJsonAgentDriver {
                     : {}),
                 telemetry: {
                     durationMs: Date.now() - startedAt,
+                    ...(result.usage ? { usage: result.usage } : {}),
                     finishReason: "completed",
                 },
                 warnings: [...result.warnings, ...snapshot.warnings],
diff --git a/dist/provider-codex/codex-json-execution-engine.d.ts b/dist/provider-codex/codex-json-execution-engine.d.ts
--- a/dist/provider-codex/codex-json-execution-engine.d.ts
+++ b/dist/provider-codex/codex-json-execution-engine.d.ts
@@ -14,5 +14,10 @@ export type CodexExecutionResult = {
     readonly outputText: string;
     readonly structuredOutput?: unknown;
+    readonly usage?: {
+        readonly inputTokens: number;
+        readonly outputTokens: number;
+        readonly totalTokens: number;
+    };
     readonly warnings: readonly {
         readonly code: string;
         readonly safeMessage: string;
diff --git a/dist/worker-codex/file-backend-codex-worker.d.ts b/dist/worker-codex/file-backend-codex-worker.d.ts
--- a/dist/worker-codex/file-backend-codex-worker.d.ts
+++ b/dist/worker-codex/file-backend-codex-worker.d.ts
@@ -1,4 +1,4 @@
-import { type ClockPort, type ObservabilityPort, type ProviderTask, type RuntimeDeps } from "@vioxen/subscription-runtime/core";
+import { type ClockPort, type ObservabilityPort, type ProviderTask, type ProviderTaskTelemetry, type RuntimeDeps } from "@vioxen/subscription-runtime/core";
 import { type CodexExecutionProfile, type CodexAppServerProcessFactory, type CodexReasoningEffort, type CodexServiceTier } from "@vioxen/subscription-runtime/provider-codex";
 import { type CapacityAwareSubscriptionWorker, type SubscriptionWorkerHealth, type SubscriptionWorkerPrewarmResult, type SubscriptionWorkerState, type WorkerCapacitySnapshot } from "@vioxen/subscription-runtime/worker-core";
 export type FileBackendCodexWorkerOptions = {
@@ -55,6 +55,7 @@ export type FileBackendCodexWorkerResult = {
 export type FileBackendCodexWorkerResult = {
     readonly outputText: string;
     readonly structuredOutput?: unknown;
+    readonly telemetry?: ProviderTaskTelemetry;
     readonly warnings: readonly {
         readonly code: string;
         readonly safeMessage: string;
diff --git a/dist/worker-codex/file-backend-codex-worker.js b/dist/worker-codex/file-backend-codex-worker.js
--- a/dist/worker-codex/file-backend-codex-worker.js
+++ b/dist/worker-codex/file-backend-codex-worker.js
@@ -456,6 +456,7 @@ export class FileBackendCodexWorker {
         return {
             outputText: result.outputText,
             structuredOutput: result.structuredOutput,
+            telemetry: result.telemetry,
             warnings: result.warnings,
         };
     }
diff --git a/package.json b/package.json
--- a/package.json
+++ b/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@vioxen/subscription-runtime",
-  "version": "0.1.0-main.2-sm.1",
+  "version": "0.1.0-main.2-sm.2",
   "description": "Provider-neutral subscription runtime for backend workers, Codex subscription execution, and pluggable session custody.",
   "license": "UNLICENSED",
   "type": "module",
