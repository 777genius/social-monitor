# Combined E3 prerequisite/schema checkpoint

Baseline b1379c8371279a42c467f37e7a23682b48bb47d8 retains accepted E2 and main90ed.
The full schema-owner decision supplies 12 legacy messages, 10 enums and field23
acquisition v2, preserving optional presence and the original two RPCs. No V3
protobuf mapping, observation RPC, producer or runtime composition is introduced.
Nine utilities retain exact donor blobs; the wire helper has the authorized stage
zero rejection and exact-two-spellings canonicality predicate. It inspects only
canonical generated bytes, removing the single empty packed finished field5 to
form Python's alternative. All original donor bytes/hashes remain unchanged.
Unknown, duplicate, reordered and malformed bytes do not match either spelling.

Current TS is generated with ts-proto 2.11.8 and grpc-tools 1.13.1/protoc 3.19.1.
Parent generated Python with grpcio/grpcio-tools 1.81.1 and protobuf 6.33.6.
Focused TS and evidence tests, strict TS, architecture, quality, line cap,
gRPC freshness, descriptor projections, OpenAPI and mobile contracts passed.
The accepted V3 codec retains its positive roundtrip and rejects legacy bytes.
Actual parent Python export-to-production-TS verification passed; the worker
also consumed that exact artifact through production admission. Local Python
still lacks google.protobuf/grpc_tools. The exact offline
image command is .cache/handoff/parent-python-checks.sh; it constructs seven
Python messages independently, checks presence/descriptors, exports actual bytes,
and feeds that exact artifact to production decodeXEvent with semantic equality
and frozen TS reencoding assertions. Six literal spellings must match; only the
finished-empty spelling has its separately frozen alternative. Both actual serializers and the production admission path are verified.
Dart is unavailable for the Flutter client gate. Full evidence and patch hashes
are in .cache/handoff/result.md and audit.json. Parent independent review, commit,
CI and later E3 integration remain separate; no runnable service is claimed.
